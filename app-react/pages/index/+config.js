// SPIKE #79: attach the probe data hook to the home route so the server logs which
// language packs composed into the cumulative `langMarker`.
export default {
  data: 'import:spike-lang/probe:probeData',
}
