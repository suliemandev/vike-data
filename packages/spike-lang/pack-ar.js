// SPIKE pack for 'ar'. A normal Vike config (what a language subpath like vike-auth/ar is)
// that contributes a marker to the cumulative `langMarker`. If effect()-driven `extends`
// works, this composes in ONLY when `lang` includes 'ar'.
export default {
  name: 'spike-lang-pack-ar',
  langMarker: ['ar'],
}
