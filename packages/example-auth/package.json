{
  "name": "example-auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Example feature extension (auth): ships its own tables via vike-data, ORM-agnostic",
  "exports": {
    "./config": "./+config.js"
  },
  "dependencies": {
    "vike-data": "workspace:*"
  }
}
