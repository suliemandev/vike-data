{
  "name": "@universal-orm/rudder",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Rudder adapter for @universal-orm/core: runs the neutral insert/find/upsert/update/delete calls against a @rudderjs/database NativeAdapter (Rudder's query builder). The app installs this one adapter; extensions never import an ORM. Rudder speaks snake_case DB names directly, so neutral keys pass straight through.",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@universal-orm/core": "workspace:*"
  },
  "peerDependencies": {
    "@rudderjs/database": ">=1.6.0"
  },
  "devDependencies": {
    "@rudderjs/database": "^1.6.0",
    "better-sqlite3": "^12.0.0",
    "postgres": "^3.4.9"
  }
}
