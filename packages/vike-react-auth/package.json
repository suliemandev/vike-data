{
  "name": "vike-react-auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "React UI tier for vike-auth: <SignInForm>, <UserButton>, and useUser() over the merged server tier (magic-link endpoints + pageContext.user). The headless core stays vike-auth; this is the per-framework wrapper.",
  "exports": {
    ".": "./index.js"
  },
  "peerDependencies": {
    "react": ">=18",
    "vike-auth": "workspace:*",
    "vike-react": ">=0.6"
  }
}
