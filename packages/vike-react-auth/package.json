{
  "name": "vike-react-auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "React UI tier for vike-auth: <SignInForm>, <UserButton>, and useUser() over the merged server tier (magic-link endpoints + pageContext.user). Localized via vike-react-i18n; ships its own translations. The headless core stays vike-auth; this is the per-framework wrapper.",
  "exports": {
    ".": "./index.js",
    "./config": "./+config.js",
    "./messages": "./messages.js"
  },
  "dependencies": {
    "vike-i18n": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18",
    "vike-auth": "workspace:*",
    "vike-react": ">=0.6",
    "vike-react-i18n": "workspace:*"
  }
}
