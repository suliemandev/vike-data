{
  "name": "vike-elements",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "vike-elements: composable UI as data. The framework-agnostic substrate for building a page out of BLOCKS — a block descriptor IR + an open registry, the definePage composer, primitive elements (text/heading/badge/divider/link, stat/markdown/custom), and the defineElement seam so any package can ship a new element with high DX. A React binding (vike-elements/react) draws the blocks; vike-view layers schema-driven blocks on top.",
  "exports": {
    ".": "./index.js",
    "./react": "./react/index.js",
    "./react/registry": "./react/registry.js",
    "./react/Blocks": "./react/Blocks.jsx"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@vike-data/kit": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18",
    "vike-react": ">=0.6",
    "vue": ">=3.5",
    "vike-vue": ">=0.9"
  },
  "peerDependenciesMeta": {
    "react": { "optional": true },
    "vike-react": { "optional": true },
    "vue": { "optional": true },
    "vike-vue": { "optional": true }
  }
}
