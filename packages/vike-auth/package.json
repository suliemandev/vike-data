{
  "name": "vike-auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Auth extension (core tier): owns the users + sessions + login_tokens tables and a server tier (magic-link sessions via universal middleware), ORM-agnostic. The composition base other extensions build on.",
  "exports": {
    ".": "./index.js",
    "./config": "./+config.js",
    "./middleware": "./vike-middleware.js",
    "./onCreatePageContext": "./oncreate.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@universal-middleware/core": "^0.4.18",
    "@vike-data/vike-schema": "workspace:*"
  }
}
