{
  "name": "vike-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vike data-layer extension: defines the inter-extension contribution point, and ships the neutral schema DSL + per-ORM compilers (at vike-data/schema, splittable into a standalone vike-schema later)",
  "exports": {
    "./config": "./+config.js",
    "./schema": "./schema/index.js"
  }
}
