{
  "name": "vike-react-themes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "React binding for vike-themes: a vike-react Wrapper that applies the configured theme (CSS variables, SSR-safe) + a runtime theme picker. Installed as an extension; the app just sets a `theme` config key.",
  "exports": {
    ".": "./index.js",
    "./config": "./+config.js",
    "./ThemeWrapper": "./ThemeWrapper.jsx"
  },
  "dependencies": {
    "vike-themes": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18",
    "vike-react": ">=0.6"
  }
}
