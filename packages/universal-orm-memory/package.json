{
  "name": "@universal-orm/memory",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "In-process memory adapter for @universal-orm/core: executes the neutral repository calls against plain Maps. Zero database, zero ORM — for tests, demos, and the proof.",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@universal-orm/core": "workspace:*"
  },
  "devDependencies": {
    "@vike-data/universal-schema": "workspace:*"
  }
}
