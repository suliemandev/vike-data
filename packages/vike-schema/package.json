{
  "name": "@vike-data/vike-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vike binding for the data layer: contributes the `schemas` cumulative config point (the inter-extension contribution point) and re-exports the framework-agnostic schema core at ./schema.",
  "exports": {
    "./config": "./+config.js",
    "./schema": "./schema.js"
  },
  "dependencies": {
    "@vike-data/universal-schema": "workspace:*"
  }
}
