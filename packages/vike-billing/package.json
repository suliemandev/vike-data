{
  "name": "vike-billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Billing / subscriptions extension. A parameterized extension: bills per-organization (composes on vike-teams) or per-user (composes on vike-auth), chosen at install time.",
  "exports": {
    "./config": "./+config.js",
    "./schemas": "./schemas.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@vike-data/vike-schema": "workspace:*",
    "vike-auth": "workspace:*",
    "vike-teams": "workspace:*"
  }
}
