{
  "name": "app-vike-blocks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal vike-blocks example: compose the built-in primitive blocks with definePage, define a custom block with defineBlock + registerBlockRenderer, and render via <Page> / <Blocks>. No schema, no data layer, no Tailwind.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "vike": "^0.4.0",
    "vike-react": "^0.6.25",
    "vike-blocks": "workspace:*",
    "vite": "^7.1.0"
  }
}
