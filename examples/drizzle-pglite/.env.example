# This example needs NO secrets to run: pglite is embedded (no DATABASE_URL), and in dev
# vike-auth prints the magic-link sign-in URL straight to the page + console, so no mail
# provider is required. Copy to .env only if you later wire a real mail transport.
