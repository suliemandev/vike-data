// GENERATED by @vike-data/vike-schema via `vike generate` — do not edit by hand.
// Re-run `vike generate` to update; the source of truth is your + config.

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Migrations {
  id Int @id
  name String @unique
  batch Int

  @@map("_migrations")
}

model Users {
  id String @id @default(uuid())
  email String @unique
  name String?
  active Boolean @default(true)
  created_at DateTime @default(now())
  updated_at DateTime @default(now())
  stripe_customer_id String?

  @@map("users")
}

model Sessions {
  id String @id @default(uuid())
  user_id String
  expires_at DateTime
  created_at DateTime @default(now())
  updated_at DateTime @default(now())

  @@map("sessions")
}

model Subscriptions {
  id String @id @default(uuid())
  user_id String
  plan String
  seats Int @default(1)
  active Boolean @default(true)
  created_at DateTime @default(now())
  updated_at DateTime @default(now())

  @@map("subscriptions")
}
